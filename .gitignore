# macOS
.DS_Store

# Xcode — per-user state, build products
build/
DerivedData/
# Packaged release artifacts (todokase CLI tarballs)
dist/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.xccheckout
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Generated Info.plist — written by generate_project.rb so build
# settings stay the single source of truth.
todarchy/Info.plist

# Xcode — local Swift Package Manager state
.swiftpm/
.build/

# Ruby (generate_project.rb deps)
.bundle/
vendor/bundle/

# Editor / tooling local state
.claude/
.idea/
.vscode/
*.swp
*~

# Design handoff (HTML prototypes, React mocks) — not needed to build
# the app and may contain unshared personal notes.
design_handoff_apple_apps/

# OS / backup / temp
*.log
*.pid
*.seed
